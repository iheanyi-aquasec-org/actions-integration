{
  "name": "rewardsservice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "container:clean": "echo hello",
    "container:build": "npm run container:clean docker build -t rewards-service ./",
    "container:run": "docker run rewards-service"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ramda": "^0.25.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  }
}
